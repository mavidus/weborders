package com.weborders.DataBase;

public class DataBaseSQL {
/*
        public void sqlData() throws ClassNotFoundException, SQLException {
            //Create Connection
            Connection con = DriverManager.getConnection("dbUrl","userid", "password");
            //Load MySql jdbs driver
            Class.forName("com.mysql.jdbc.Driver");
            Statement stmt = con.createStatement();

            String query=stmt.executeQuery(select *  from employee;);
            String dbUrl="******";
            String username="Emel";
            String password="password";
            //Since you are using configurations.properties you can get user name and password from there.

            // Execute the SQL Query. Store results in ResultSet
            ResultSet rs= stmt.executeQuery(query);

            // While Loop to iterate through all data and print results
            while (rs.next()){
                String OrderName = rs.getString(1);
                String OrderNum = rs.getString(2);
                System. out.println(OrderName+"  "+OrderNum);
            }
            // closing DB Connection
            con.close();
        }
*/
}

